/* ═══════════════════════════════════════════════════════════════════
   JDM RESTORATION GARAGE — utils.js  (Cluster 01)
   Utility namespace — functions added in later clusters.
   ═══════════════════════════════════════════════════════════════════ */

(function () {
  'use strict';

  window.Utils = {
    // Populated by later clusters.
    // e.g. Utils.formatYen(n), Utils.conditionClass(v), Utils.uuid(), …
  };

}());
